(arquivomorto/ferramentas-biblicas.html and arquivomorto/manual-ferramentas-biblicas.html are HTML source files, not build artifacts or temporary files, so no additional ignores are needed beyond what might already exist)